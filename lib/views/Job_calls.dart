import 'package:flutter/material.dart';

class JobCalls extends StatelessWidget {
  const JobCalls({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Job Calls')),
      body: const Center(child: Text('Job Calls Screen')),
    );
  }
}
